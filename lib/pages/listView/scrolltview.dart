import 'package:flutter_single_app/import_common.dart';

class ScrollList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('ScrollList'));
  }
}
